import { ComingSoon } from '../../components/ComingSoon'

// Filled in during Stage 8 (admin real-time pipeline/tracking dashboard).
export function DashboardPage() {
  return (
    <ComingSoon
      title="Pipeline dashboard"
      description="Real-time overview of every project's workflow state, stalled stages, and retry controls."
    />
  )
}
